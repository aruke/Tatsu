<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>
        <variable name="viewModel" type="org.rionlabs.tatsu.ui.screen.main.timer.TimerViewModel"/>
    </data>

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            tools:context=".ui.screen.main.timer.TimerFragment">

        <TextView
                android:id="@+id/textTimerStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:drawableTop="@drawable/ic_timer"
                android:layout_margin="16dp"
                android:tint="@color/colorPrimary"/>

        <include
                android:id="@+id/viewTimerDigits"
                layout="@layout/view_timer_digits"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                bind:duration="@{viewModel.duration}"/>

        <Button
                android:id="@+id/buttonTimer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="16dp"
                android:text="@string/start_timer"/>

    </LinearLayout>
</layout>